pipeline {
    agent none 
    stages {
        stage('Example Build') {
            steps {
                sh 'date'
            }
        }
        stage('Example Test') {
            steps {
                sh 'java -version'
            }
        }
    }
}
